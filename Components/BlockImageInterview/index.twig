<flynt-component load:on="visible" name="BlockImageInterview" class="interviewComponent min-w-fit">
  <div class="py-sm lg:px-lg lg:py-0">
    <figure class="figure {{ imageHeight }}">
      {% if image %}
        <img class="figure-image lazyload h-full w-auto object-cover"
          src="{{ image.src|resize(32) }}"
          data-srcset="{{function('wp_get_attachment_image_srcset', image.id)}}"
          data-sizes="auto"
          alt="{{ image.alt|e }}">
      {% endif %}
      {% if image.caption %}
        <figcaption class="caption">{{ image.caption|e }}</figcaption>
      {% endif %}
    </figure>
  </div>
</flynt-component>
