<flynt-component client:idle name="NavigationMain" class="hidden md:block relative">
  <nav class="navigation w-screen p-sm mx-auto z-[99]" aria-label="{{ labels.ariaLabel|e }}">
    <div class="flex flex-col align-middle justify-center gap-sm h-full">
      {% if menu %}
        {% include 'Partials/_menu.twig' with { 'items': menu.items } only %}
      {% endif %}
    </div>
  </nav>
</flynt-component>
